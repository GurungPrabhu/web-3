import { Button } from "@elements";
import React from "react";

const Header: React.FC = () => (
  <div className="header">
    <Button type="primary"> My Wallet </Button>
  </div>
);

export { Header };
